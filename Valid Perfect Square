class Solution {
public:
    bool isPerfectSquare(int num) {
        for (int i = 1; i < 46341 && i <= num; ++i) {
            if (i * i == num)
                return true;
        }
        return false;
    }
};
