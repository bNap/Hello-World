class Solution {
public:
    string reverseString(string s) {
        int num=s.size()-1;
        string result;
        for(int i=0;i<=num;i++){
            result+=s.at(num-i);
        }
        return result;
    }
};
